# Copy this file to ".env" for local development.
# These defaults keep the dev server working without any external credentials.

# Server settings
PORT=3000

# OAuth portal configuration (the mock values keep log-in links working locally)
VITE_OAUTH_PORTAL_URL=http://localhost:4173/mock-oauth
VITE_APP_ID=local-dev-app

# Forge proxy configuration for Google Maps.
# Leave the dummy key unless you have a real Forge proxy; the map component will
# automatically show a placeholder if the key endpoints are unreachable.
VITE_FRONTEND_FORGE_API_KEY=dummy-forge-key
VITE_FRONTEND_FORGE_API_URL=https://forge.butterfly-effect.dev

# Analytics snippet – blank values disable the script during local dev.
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
